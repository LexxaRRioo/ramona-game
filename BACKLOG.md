Ideas checklist - add as they come up, check off once shipped (note the version). Not a roadmap; plan.md's phases are the roadmap. This is the "someday" pile.

## Reference

- `cat_pack/black cat with text.png` is a labeled reference sheet for the same 14-col grid as `ramona_sheet.png`/`cat_pack/cat 1 (64×64).png` (row indices line up, at least through the low-numbered rows checked so far - it has 66 rows vs. the other two's 72, so don't assume alignment holds at the tail without checking). Its labels are WHITE text on a transparent background - invisible composited onto white, only legible composited onto a dark background. Use this before guessing at a pose from silhouette alone.

## Animation / sprite variety

- [x] Fix wrong grooming pose: row 17 (previously wired as `groom`) is actually labeled "Scratch (sit, left)" in the reference sheet - raises the paw above her head, not a wash. Row 12 ("Lick paw sit front") is the real grooming motion, paw stays at face height. Swapped - pending release.
- [x] Add a rare (~5%) scratch variant to `.groom` using row 17/18 ("Scratch (sit, left/right)") - the pose I originally mistook for grooming. Pending release.
- [x] Slow the grooming loop down with a pause between wash cycles (`CatClip.loopPause`) instead of an unbroken loop - pending release.
- [x] Real run/leap animation for .seekAttention (rows 10/11) instead of a sped-up walk cycle - 0.1.2. Speed bumped from 2x to 4x walkSpeed - pending release.
- [x] Grooming (lying variant): row 13, "Lick paw lie front" - same wash motion as the main `groom`, but lying down. Plays instead of the sitting groom when she was just sleeping (previousAction == .sleep) - pending release.
- [x] Seek-attention variant: sits in a far corner and meows - confirmed row 14 "Meow sit front" (3 frames) against the labeled reference sheet. Added `CatSprites.meowSit`; a ~20% chance on entering `.seekAttention` plays it in place instead of the run/leap (`CatScene.meowChance`) - pending release, chance untuned/guessed.
- [ ] Seek-attention variant: lies down/leaps in either direction - candidate is a mirrored leaping/stretching pose (2 frames, L/R) surfaced during a prior session's screenshots. Searched the full reference sheet this session (all 66 rows, `black cat with text.png`) and didn't find a clean 2-frame mirrored leap/stretch match - closest candidates are "Jump (left)"/"Jump (right)" (rows 63/64, 5 frames each, mid-air pose at frame 2) and the existing row 10/11 run/leap already used elsewhere, neither matching "2 frames" from the original note. Left unchecked rather than guessing; needs the original screenshot re-examined or a fresh look to pin the row.
- [ ] Idle pose additions - confirmed this session against `black cat with text.png` that rows 44-55 (the "Sleep 1-5" sets below) are almost certainly what these refer to: each Sleep N is 2 frames (front/back) x left/right, i.e. exactly "lying-down idle, 2 mirrored directions" territory. No separate "alert sit, tail up, wide-eyed" row turned up anywhere in the sheet (rows 0-65 fully scanned) - doesn't appear to exist in this pack.
- [x] Sleep pose rows identified: 44-55 are "Sleep 1" through "Sleep 5", each front/back x left/right, 2 frames per pose (confirmed via labeled reference sheet, not the earlier "looked like leaping/running" guess).
- [ ] Sleep pose variety (rows 44-55, "Sleep 1" through "Sleep 5" per the reference sheet - 5 distinct curled/sprawled loaf poses, front/back x left/right, 2 frames each, unused) - skipped for 0.1.2: the single lie-down transition animation only curls into one specific final pose, so swapping the held pose afterward pops. Would need either a matching transition per pose, or a plain cross-fade/cut, to do without a visible seam.
- [x] Dedicated drag/held sprite - rows 60/61 ("Hiss (front, left/right)") plays on pickup instead of freezing on whatever frame she was on - pending release.
- [x] Directional sit/stand statics - row 0 cols 2/3 ("Sit left/right") now play when she stops right after walking/climbing/seeking, instead of always the front-facing sitDown/sitIdle pair. Rows 1 (stand) and the rear/other row-0 columns are still unused.
- [ ] Correction: rows 29-42 are NOT side-profile run cycles as originally guessed here - confirmed against the labeled reference sheet this session, they're paw-swipe/batting animations (left/right paw x stand/sit x front/back/left/right, 11 frames each). Could make a nice "playing/batting at something" idle variant later, but isn't a run cycle and isn't currently requested content.
- [ ] Future "playing with an object" action/variant: use rows 29-30, 32-37, and 39-42 (the paw-swipe/batting family above, minus 31/38 - see the climb entry below, which uses that same family's "(back)"-view rows instead).
- [ ] Happy-mood action: "massaging with paws and claws" (kneading) - row 2 (4 frames, front-view walking-toward-camera pose with alternating front paws, reads as a kneading motion). Ties into the still-unimplemented "real per-mood animations" (mood currently only tints color, see CatScene.apply's doc comment) - needs that mechanism to exist before this can be wired in as a happy-specific action/flourish rather than a new CatAction.
- [ ] Seek-attention variants: rows 14 (meowSit, already used), 15 ("Meow lie front", 3 frames), 16 ("Meow stand front", 3 frames) are all meow poses in different postures - use 15/16 as additional seekAttention variants alongside the existing meowSit, not just the one.
- [x] Climb animation: row 62 ("Jump (back)", 3 frames, rear-view one-paw reach) replaces the reused walk cycle - `CatSprites.climb`, played throughout the ascent instead of pacing afterward. Chosen from a side-by-side GIF comparison against rows 29/30/31 (same reach-up pose family, different cycle lengths). Movement itself is still a single diagonal glide to the window (settle), not yet the "series of vertical jumps" mentioned in discussion - that's a separate follow-up, not done this pass.
- [ ] Recolor pass: check across sprites/rows that the white bib is actually visible (may be getting lost/miscolored on some poses). Also a specific recoloring bug - the back right leg is coming out half-white instead of fully matching Ramona's one-white-leg marking - needs a fix in the recolor pipeline, not just a one-off frame touch-up.
- [x] Row 43 (`sitDown`, already shipped) coloring bug: frames 2 and 7 (0-indexed cols 1 and 6) of the 7-frame yawn/sit-down animation were missing almost all of their white bib/front-leg marking (12 white px vs. 46-86 in the other 5 frames) - a visible flicker every cycle. Fixed by copying the white-pixel mask from a correctly-marked frame onto those two (the pose's torso/legs don't move during a yawn, only the head/mouth does, so this was safe) - verified on-palette and visually consistent across all 7 frames afterward.
- [ ] Rows 16/17 white-marking check (requested alongside the row 43 fix, "only when used somewhere"): row 17 (`scratch`, already used) - checked white-pixel counts across all 8 frames, all in the 44-51px range with no frame dropping out like row 43's did, so the same "missing marking" bug doesn't appear to be present. Row 16 isn't used anywhere yet, so left unexamined per the "only when used" condition. If there's a specific frame/screenshot that still looks off, needs a pointer to it rather than another blind pixel scan.

## Bugs (known, not yet fixed)

- [x] Lie-down/curl-into-sleep transition should pick one side (left-curl or right-curl) at random each time and stick to frames from only that side - row 6's original 10-frame sequence mixed both curl directions. Split into `lieDownLeft`/`lieDownRight` (+ matching `sleepLeft`/`sleepRight`), one picked at random per settle - pending release.
- [x] Ramona sits in front of the Dock instead of on it - the Dock's AXList frame from Accessibility reports ~5pt more height than the strip's actual rendered top edge (confirmed via screenshot pixel-sampling), so she settled floating just above/in front of the visible glass. Fixed with a measured correction in `Dock.bottomFrame()` - pending release.

- [ ] Window/Dock floor tracking is fragile and keeps regressing - test-covered and fixed this session, but NOT yet verified live (leave unchecked until confirmed - see last bullet below):
  - Root cause 1 (window tracking): `FrontmostWindowTracker` only ever tracked "whichever window is currently frontmost", with no concept of window identity - so if she'd climbed onto window A and the user then focused window B (without moving A), the tracker silently started reporting B's frame through the exact same callback used for "your window moved", and she'd follow it. Fixed by having the tracker compare AXUIElements across reports (`CFEqual`) and pass an `isSameWindow` flag through `onFrameChange`; `FloorTracking.nextSurface` (new, pure, unit-tested in `FloorTrackingTests`) decides the resulting surface, and `CatScene` uses `isSameWindow` to pick the animation - a same-window move re-settles normally, a different window becoming frontmost plays an explicit walk-style jump (`playJumpToCurrentSurface`) instead of silently gliding across in whatever pose she was in.
  - Root cause 2 (Dock sinking/floating): `Dock.topPaddingCorrection` was a fixed 5pt guessed from one machine's Dock. The padding above the Dock's visible icons scales with icon size (System Settings > Dock > Size), so a constant can't generalize - this dev machine's Dock (tile size 66, magnification on) is a different config than whatever 0.2.0 was tuned against. Replaced with a direct measurement: `Dock.bottomFrame()` now unions the individual icon elements' own AX frames (median of their top edges, robust to a single magnified/hovered outlier) instead of applying a constant to the outer list frame - see `Dock.strip`/`Dock.median`, unit-tested in `DockGeometryTests`.
  - Extracted `Surface`/`FloorTracking` (window > Dock > floor ground-line priority, perch validity, and the surface-transition decision) out of `CatScene` into a plain, AppKit/SpriteKit-free type specifically so this logic has real test coverage instead of only being checkable by eyeballing the running app.
  - **Not independently verified live**: pixel-level Dock alignment needs Accessibility permission granted to a running build, which isn't something achievable from this sandboxed session - build via `Scripts/build-dev-app.sh`, grant Accessibility once to `.build/Ramona.app`, and confirm she now sits cleanly on the Dock strip (not floating or sinking) and correctly jumps between windows as focus changes.
- [x] Debug menu "Force Action": confirmed via code reading (not just inspection - traced the full call chain) that it already applies immediately - `Picker.onChange` -> `AppDelegate.forceAction` -> `BehaviorEngine.setForcedAction` -> `evaluateAction()` -> `onStateChange` -> `CatScene.apply` all run synchronously on the main thread with no Timer/dispatch in between. No fix needed - pending release.
- [x] Dev-only toggle to hide the sprite/overlay entirely while Claude is working in the repo - `RAMONA_HIDDEN=1` env var now skips creating the overlay window in `AppDelegate.applicationDidFinishLaunching`, while the behavior engine/accessibility flow still start normally - pending release.
- [x] Debug menu option to auto-cycle through every CatAction on a timer (walk, idle, sleep, groom, seekAttention, climb, ...) - added "Auto-Cycle Actions (QA)" toggle, steps through `CatAction.allCases` every 5s via `AppDelegate.setAutoCycle` - pending release.
- [x] Leaving `.climb` for any other action (found via manual climb -> sleep testing: she fell to the Dock, curling up mid-fall) unconditionally reset `currentSurface` to `.floor` in `CatScene.apply`, racing the next action's own settle - she should be able to rest/groom/idle/walk on whatever surface she climbed to, not just the floor. Removed the forced reset entirely; she now stays on the window until something actually displaces her (it closes/moves off-screen, or a manual drop) - pending release.

## Behavior / feel

- [x] Fix sleeping-cat drift on repeated settle replay (catVisual child-node offset, decoupled from cat.position) - 0.1.2.
- [x] Reduce the periodic "disturbing" re-settle replay to a random 5% chance per 20s tick instead of unconditional - 0.1.2.
- [x] Fix animation freezing after picking her up mid-groom (or any action) and dropping her - this was a regression from the 5%-flourish change above: hold-end's catch-up tick got caught by the same gate meant for the passive ambient tick. Added `BehaviorEngine.resumeAfterHold()`, which always resyncs the view. Pending release.
- [x] Climb targeting: she now actively prefers heading for the nearest reachable highest point instead of only climbing when the utility AI happens to pick .climb regardless of height. Added `FloorTracking.offersHigherPerch` (pure, unit-tested - true only when the tracked window is a valid, strictly-higher perch than wherever she's currently standing) and `CatScene.higherPerchAvailable`, fed into a new `BehaviorEngine.setHigherPerchAvailable` alongside the existing `setWindowAvailable` gate; `CatAction.climb.score` adds a flat `climbPreferenceBoost` (0.3, tuned to clear actionSwitchMargin against idle regardless of trait weights) when set. Wired from `AppDelegate.startWindowTracking`'s existing `onFrameChange` callback, so it updates whenever the tracked window's frame changes (launch, focus switch, resize/move) - doesn't recompute on Dock show/hide or after a manual drag-drop, since those don't fire that callback; noted as a known gap rather than plumbed further this pass. Watched live: leaving `.climb` for any action used to force her back to `.floor` mid-transition (see the sleep-on-any-surface fix below) - fixed. Climb now has a dedicated animation (row 62, `CatSprites.climb`) instead of reusing walk, and the ascent itself plays as a short series of upward hops (`CatScene.climbAscent`, 1-4 hops depending on vertical distance) instead of one smooth diagonal glide, which read as floating/gliding up rather than an active climb - pending release.
- [ ] When sitting/standing on a window (or the menu bar strip) would leave her partially hidden behind other UI on top of it (screenshot this session: sitting on the menu bar, a status item/notch icon overlapping and clipping her), she should lie down instead - a lying pose takes less vertical height, so it's more likely to fit fully visible in whatever gap she's perched in. Needs a way to detect "would I be occluded here" (or at least "is there enough clear vertical room in this gap") and a lying-idle pose to switch to - ties into the unused lying-idle frames noted in the Animation section above.
- [x] Rework mouse interaction: left click (and click-hold-move) should be petting/cursor-petting, not pickup. Right click should move/drag her instead. Swapped `CatScene`'s handlers - `mouseDown`/`mouseDragged`/`mouseUp` (left) now pet on click-release and repeatedly (throttled to petPulse's 0.3s cadence) while held-and-moved; `rightMouseDown`/`rightMouseDragged`/`rightMouseUp` carry the pickup-drag mechanics that used to live on the left button, unchanged otherwise - pending release.
- [x] Disable leg movement during playJumpToCurrentSurface's jump (and any future jump animations) - hold a static leap pose instead of cycling the run clip's legs mid-air. Added `CatSprites.leapRight`/`leapLeft`, the run cycle's own col-1 mid-leap frame held static, swapped in for the jump only (seekAttention's run loop is untouched) - pending release.
- [x] Behavior tuning pass: 3x less walking, 2x more sleeping, 1x more grooming - relative frequency, not absolute. Added `CatAction.walkFrequencyMultiplier` (1/3) and `sleepFrequencyMultiplier` (2x), applied to the final walk/sleep scores; groom's formula is untouched since walk dropping and sleep rising against it already shifts its relative win-rate up ("1x more" read as no direct change needed). `NeedsState.decay` left alone - pending release, ratios untested live.

## Distribution / infra

- [ ] CI/CD GitHub Action to automate the release pipeline (build dmg, quarantine smoke test, sign appcast, gh release create) - outlined in conversation, not implemented. Open decision: export the Sparkle EdDSA private key to GitHub Actions Secrets, or keep the signing step manual.
- [ ] Accessibility permission re-prompt on every release build - root cause is TCC scoping grants to the ad-hoc code signature, which changes every rebuild. Only real fix is a stable paid Developer ID cert (signing release builds like the dev build's persistent "Ramona Dev" cert doesn't help - that's a separate trust mechanism from Gatekeeper's unnotarized-app prompt).
- [ ] "Unable to Check For Updates" / "The updater failed to start" alert popped up on launch after enabling automatic update checks - seen while testing via plain `swift run` (which I recommended this session for a one-off Accessibility-permission check, since no dev signing identity existed yet). Sparkle needs a real .app bundle with an Info.plist (CFBundleIdentifier, SUFeedURL, etc.) to initialize - `swift run` produces a bare executable with none of that, unlike `Scripts/build-dev-app.sh`'s bundle or a real release build, so this is likely a `swift run`-only rough edge rather than a bug in what actually ships. Worth confirming Sparkle behaves on a properly bundled dev/release build, and either fixing it there if it also occurs, or noting in README/CLAUDE.md that `swift run` isn't a full test of Sparkle-dependent behavior.
- [x] generate_appcast clobbering: added `Scripts/update-appcast.sh <version> [dmg-path]`, which runs generate_appcast against a throwaway directory containing only the new dmg (so it can only ever emit one entry, nothing else to clobber), then merges just that entry into appcast.xml with the correct GitHub-release enclosure URL and release-notes/ path, leaving every other entry untouched. Verified against a scratch copy of appcast.xml with a real (non-production) signed test dmg before trusting it - inserted cleanly, existing entries unchanged. Not yet wired into build-release-dmg.sh or run for a real release.

## Testing

- [x] `BehaviorEngine` test coverage: it owned `Date()`/`Timer`/`Double.random` directly with no injectable seam, so the tick loop and evaluateAction's switch-margin/flourish logic couldn't be tested deterministically - also risked writing to the real save file on disk (`CatSaveState.persist()`) if ever exercised under `swift test` locally. Added `now`/`randomDouble`/`persist` closure params (all defaulting to the real Date/random/disk behavior, so `AppDelegate`'s call site is unchanged) and `Tests/RamonaTests/BehaviorEngineTests.swift`, covering: tick's elapsed-time decay, the switch-margin threshold (no-switch vs. switch), the flourish-chance re-confirmation roll, `resumeAfterHold`'s unconditional notify, `setForcedAction`'s override, and `pet()`'s wiring. Still untested: `ItemDefinition.loadAll()` (bundled JSON, same shape-mistake risk `SpeciesDefinitionTests` guards against) and `FrontmostWindowTracker`'s own AXObserver wiring (app/window-focus notifications, `CFEqual` identity comparison) - unlike Dock, this is direct imperative glue over live AXObserver callbacks rather than a data-in/data-out function, so it wasn't given the same fake-seam treatment; a similar abstraction is possible but is a bigger lift for a much smaller, lower-risk surface (the actual decision logic it feeds, `FloorTracking.nextSurface`, is already tested).

## Content (per plan.md scope)

- [ ] Lola (second cat) - new Species JSON + sprite folder, no code change per the locked architecture decision.
- [ ] Two-cat interactions - explicitly deferred in plan.md's V1 scope.
